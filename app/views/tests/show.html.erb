<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Test Details</title>
</head>
<body>
<h1>Test Details</h1>

<% if @test %>
  <table>
    <tr>
      <th>ID</th>
      <td><%= @test.id %></td>
    </tr>
    <tr>
      <th>Title</th>
      <td><%= @test.title %></td>
    </tr>
    <tr>
      <th>Level</th>
      <td><%= @test.level %></td>
    </tr>
    <tr>
      <th>Category</th>
      <td><%= @test.category.title if @test.category %></td>
    </tr>
  </table>
<% else %>
  <p>Test not found. (ID: <%= params[:id] %>)</p>
<% end %>

<a href="/tests">Back to Tests List</a>
</body>
</html>